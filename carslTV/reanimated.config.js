module.exports = {
    // Disable strict mode warnings about reading/writing shared values during render
    // This is a known issue with certain libraries that use reanimated internally
    disableNativeInitialization: false,
};
